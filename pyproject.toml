[project]
name = "mpc-sim"
version = "0.1.0"
description = "Working version of MPC with MuJoCo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mediapy>=1.2.4",
    "mujoco>=3.3.3",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
